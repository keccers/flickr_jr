<div class="nav">
  <h1> The Burning House</h1>
  <h4><a id="login-show" href="/login">|| Login</a></h4> 

  <form id="login" action="" method="POST">
  <%= erb :_login_form, layout: false %>
</form>
<% if current_user %>
  <%= erb :_photo_form, layout: false %>
<% end %>
</div>

</div>

<div class="container">
  <% if @photos %>
  <% @photos.each do |photo| %>
  <div class="photo">
    <img src="<%= photo.photo.url %>" alt="">  
  </div>
  <% end %>
<% end %>




